int count_positives(const int size, const int array[]){
    if(array == NULL){
        return -1;
    }
    int number = 0;
    for(int i = 0; i < size; i++){
        if(array[i] > 0){
            number++;
        }
    }
    return number;
}
