int count_whites(const char string[]){
    if(string == NULL){
        return -1;
    }
    int positive = 0;
    for(int i = 0; i < strlen(string); i++){
        if(string[i] == ' ' || string[i] == '\t' || string[i] == '\n'){
            positive++;
        }
    }
    return positive;
}
